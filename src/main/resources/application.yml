
quarkus:
   datasource:
      db-kind: mysql 
      jdbc:
        driver: com.mysql.jdbc.Driver
        url:  jdbc:mysql://10.10.12.100:9000/test
      username: mm
      password: PP@@
# drop and create the database at startup (use `update` to only update the schema)
   hibernate-orm:
      database:
         generation: update
